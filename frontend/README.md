# Frontend

Next.js (App Router), Tailwind, React Hook Form + Zod, Sonner.

Copie `.env.local.example` para `.env.local` e defina `NEXT_PUBLIC_API_URL` (URL da API). No backend, `FRONTEND_ORIGIN` deve bater com a URL do Next.

```bash
npm install
npm run dev
```
